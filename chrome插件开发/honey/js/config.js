var _glbl_config = {
    // 0 -> in developer mode
    // 1 -> no longer in developer mode
	isDevMode: 0
    // url of the groups path
    // main code will handle domain prepending
    , groupsURL: 'home/#groups'
};
